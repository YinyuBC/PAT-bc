#include<iostream>
#include<map>
using namespace std;

int main(){
    string a;
    map<char,int> m;
    cin>>a;
    for(int i=0;i<a.size();i++)
        m[a[i]]++;
    for(auto it=m.begin();it!=m.end();it++)
        cout<<it->first<<":"<<it->second<<endl;
    return 0;
}
